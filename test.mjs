/**
 * Method return sum of two elements
 * @param {*} a is a first element
 * @param {*} b is a second element
 * @returns sum of two elements
 */
function sum (a, b) {
    return a + b;
}

sum(2, 3);

export default sum;